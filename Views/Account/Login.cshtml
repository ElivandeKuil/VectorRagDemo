@model VectorRagDemo.Models.ViewModels.LoginViewModel
@{
    ViewData["Title"] = "Inloggen";
}

<div class="row justify-content-center">
    <div class="col-md-5">
        <div class="card shadow-sm">
            <div class="card-header bg-primary text-white">
                <h4 class="mb-0">
                    <i class="bi bi-box-arrow-in-right me-2"></i>Inloggen
                </h4>
            </div>
            <div class="card-body">
                <form asp-action="Login" asp-route-returnUrl="@ViewData["ReturnUrl"]" method="post">
                    @await Html.PartialAsync("_ValidationSummary")

                    <div class="mb-3">
                        <label asp-for="GebruikersNaam" app-label></label>
                        <div class="input-group">
                            <span class="input-group-text">
                                <i class="bi bi-person"></i>
                            </span>
                            <input asp-for="GebruikersNaam" app-field autofocus />
                        </div>
                        <span asp-validation-for="GebruikersNaam" class="text-danger"></span>
                    </div>

                    <div class="mb-3">
                        <label asp-for="Wachtwoord" app-label></label>
                        <div class="input-group">
                            <span class="input-group-text">
                                <i class="bi bi-lock"></i>
                            </span>
                            <input asp-for="Wachtwoord" app-field />
                        </div>
                        <span asp-validation-for="Wachtwoord" class="text-danger"></span>
                    </div>

                    <div class="mb-3 form-check">
                        <input asp-for="RememberMe" app-field />
                        <label asp-for="RememberMe" app-label="check"></label>
                    </div>

                    <div class="d-grid">
                        <button type="submit" btn-variant="primary" btn-icon="box-arrow-in-right" class="me-2">Inloggen</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script src="https://cdn.jsdelivr.net/npm/jquery-validation@1.19.5/dist/jquery.validate.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/jquery-validation-unobtrusive@3.2.12/dist/jquery.validate.unobtrusive.min.js"></script>
}
