@{
    ViewData["Title"] = "Privacyverklaring";
    Layout = "_Layout";
}

<div class="row justify-content-center">
    <div class="col-lg-8">
        <div class="mb-3">
            <a asp-controller="Account" asp-action="Login" btn-variant="outline-secondary" btn-size="sm" btn-icon="arrow-left">Terug naar inloggen</a>
        </div>

        @await Html.PartialAsync("_PrivacyVerklaringInhoud")
    </div>
</div>
